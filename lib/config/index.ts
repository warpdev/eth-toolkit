// Application-wide configuration

// Example of application config (replace with actual values)
export const config = {
  appName: 'Ethereum Developer Toolkit',
  version: '2.0.0',
  apiEndpoints: {
    // Add API endpoints as needed
  },
  storage: {
    prefix: 'eth-toolkit-',
  },
};